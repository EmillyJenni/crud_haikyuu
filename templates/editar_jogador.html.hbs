<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>{{ title }}</h1>
    <form action="/atualizar_jogador/{{ jogador.id }}" method="post">
        <label>Nome: <input type="text" name="nome" value="{{ jogador.nome }}" required></label><br>
        <label>Sobrenome: <input type="text" name="sobrenome" value="{{ jogador.sobrenome }}" required></label><br>
        <label>Altura: <input type="text" name="altura" value="{{ jogador.altura }}" required></label><br>
        <label>Posição: <input type="text" name="posicao" value="{{ jogador.posicao }}" required></label><br>
        <label>Escola:
            <select name="escola_id" required>
                {{#each escolas}}
                    <option value="{{ this.0 }}" {{#if (eq this.0 jogador.escola_id)}}selected{{/if}}>{{ this.1 }}</option>
                {{/each}}
            </select>
        </label><br>
        <label>Login: <input type="text" name="login" value="{{ jogador.login }}" required></label><br>
        <label>Senha: <input type="password" name="senha" required></label><br>
        <button type="submit">Salvar Alterações</button>
    </form>
    <nav>
        <ul>
            <a href="/listar_jogadores">Voltar</a>
        </ul>
    </nav>
</body>
</html>